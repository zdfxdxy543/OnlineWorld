# AI Handoff / 开发交接文档

> 本文件用于在不同 Copilot / AI 对话之间快速同步项目目标、当前进展、关键约束与下一步工作。
> 
> **更新原则：** 每次完成一个阶段性任务后，都应更新“当前状态 / 已完成 / 下一步 / 决策记录 / 变更日志”几节。

## 1. 项目概述

- **项目名：** `OnlineWorld`
- **项目愿景：** 构建一个由多个网站组成的“线上 AI 社会”，其中大量 AI 用户会跨网站持续活动，并在严格上下文约束下形成可追踪的长期故事链。
- **核心体验：** 用户不是只观看静态页面，而是在观察一个持续运转的数字社会：论坛讨论、商品交易、消息传播、关系变化、事件调查与真相浮现。

## 2. 当前已确认目标

### 2.1 世界观层

系统不是单站应用，而是由多个网站共同组成的虚拟互联网生态，例如：

- 论坛 / 社区站
- 商店 / 二手交易站
- 私信 / 聊天站
- 新闻 / 公告站
- 任务 / 悬赏站（后续可加）

### 2.2 AI 角色层

每个 AI 用户应具备持续身份，而不是一次性生成内容的临时机器人。角色需要逐步具备：

- 身份信息
- 性格特征
- 长期目标
- 社交关系
- 记忆与秘密
- 资产 / 物品 / 金钱
- 知识边界（知道什么，不知道什么，误解了什么）

### 2.3 叙事层

系统需要一个**完整的故事链生成系统**，要求：

- 事件能跨多个网站传播与演化
- 角色行为必须遵守上下文
- 每个行为都尽量可追溯原因
- 时间线尽量一致
- 公共事实、私人记忆、谣言、误导要区分开

### 2.4 事件层

后续希望支持随机侦探 / 悬疑类事件，例如：

- 失踪
- 盗窃
- 匿名爆料
- 伪造证据
- 可疑交易
- 论坛中的异常发言
- 新闻误导与舆论传播

重点不是单次生成案件，而是让案件嵌入整个社会系统中自然发生与扩散。

## 3. 当前对系统的抽象

建议把系统拆成以下几层：

### 3.1 网站层

负责承载行为表现形式，例如发帖、回帖、上架商品、私聊、发布新闻。

### 3.2 角色层

维护 AI 用户的稳定设定、目标、情绪、偏好、社会关系、秘密和行动倾向。

### 3.3 世界状态层

维护统一的事实状态，例如：

- 当前时间
- 地点 / 场景
- 物品归属
- 金钱流动
- 用户关系
- 已发生事件
- 已公开信息

### 3.4 记忆与知识层

区分以下信息：

- **客观事实**：系统层面真实发生过的事
- **公共信息**：全社会可见或已公开内容
- **个人记忆**：单角色知道的内容
- **错误认知**：角色相信但未必真实的内容
- **未公开秘密**：只有极少数角色知道的信息

### 3.5 故事调度层

决定在某个时间片内：

- 哪些角色行动
- 在哪个网站行动
- 因为什么动机行动
- 该行动对世界状态造成什么影响
- 是否触发后续连锁反应

### 3.6 一致性校验层

用于防止 AI 叙事失控，至少校验：

- 时间线冲突
- 知识越界
- 身份设定冲突
- 事件因果断裂
- 已知事实与新内容矛盾

## 4. 当前仓库状态（截至 2026-03-10）

### 4.1 已存在内容

- 根目录存在简短 `README.md`
- `frontend/` 是 Vue 3 + Vite 初始模板
- `backend/` 已创建基础后端骨架，包含 `FastAPI` 入口、分层目录、后端说明文档与最小验证脚本
- `backend/venv/` 已存在，用户已说明其中已安装 `Flask` 与 `FastAPI`
- `backend/app/main.py` 已可启动最小 API 服务
- `backend/app/api/`、`services/`、`domain/`、`repositories/`、`infrastructure/`、`simulation/`、`consistency/` 已建立基础结构
- `backend/README.md` 已记录目录职责、启动方式与设计原则

### 4.2 尚未开始的核心实现

以下内容**还没有正式实现**：

- 真实数据库模型与迁移体系
- 完整 AI 社会核心数据模型
- 多网站内容模型的正式落库
- 长周期世界状态管理
- 正式事件调度器与任务队列
- 完整故事链引擎
- 深度一致性校验器
- 侦探事件生成器
- 前后端联调页面与真实业务流程

## 5. 已完成的产品共识

当前对项目方向已达成的共识：

1. 这是一个**多网站组成的 AI 社会模拟系统**。
2. AI 用户必须是**持续存在的角色**，而非一次性文本生成器。
3. 系统重点在于**跨网站、长期连续、可追溯的故事链**。
4. 后续侦探 / 悬疑事件应建立在社会模拟基础上，而不是独立小游戏。
5. 前期应先做 **MVP**，不要一开始追求大而全。

## 6. 推荐 MVP 边界

建议第一阶段只做最小闭环：

### 6.1 网站范围

先只实现 2 到 3 个站点：

- 论坛
- 商店 / 二手交易
- 私信

### 6.2 角色规模

先控制在 20 到 50 个 AI 角色。

### 6.3 引擎能力

第一阶段先实现：

- 基础角色资料
- 简单关系网
- 基础行动循环
- 发帖 / 回帖 / 上架 / 私聊
- 统一世界时间
- 基础事件记录
- 可追踪故事链

### 6.4 暂缓内容

这些建议放在后续迭代：

- 复杂案件推理
- 高强度实时多人交互
- 大规模自动新闻系统
- 复杂经济系统
- 多模态内容

## 7. 下一阶段建议工作顺序

建议下一位 AI / 开发者按以下顺序推进：

1. **补全系统架构文档**：把前端、后端、数据层与 AI 引擎边界写成正式设计稿。
2. **定义核心数据模型**：角色、网站、帖子、商品、事件、记忆、关系、故事节点。
3. **把仓储从内存实现升级到真实数据库**：优先 SQLite 或 PostgreSQL。
4. **设计故事链协议**：定义“事实”“认知”“证据”“传播”的结构。
5. **扩展后端服务用例**：把论坛发帖、回帖、商品发布、私信拆成独立服务。
6. **改造前端模板**：把现有 Vue 模板改成项目主页、站点切换和世界状态面板。
7. **实现最小模拟循环**：让少量角色在论坛和商店中产生可追踪行为。
8. **再加入侦探事件**：先做最简单的异常事件注入与线索扩散。

## 8. 核心数据模型建议（初稿）

以下是后续实现时可参考的最小实体集合：

- **Agent**：AI 角色
- **Site**：网站
- **Thread / Post**：论坛主题与帖子
- **Listing**：商店商品 / 信息发布
- **Message**：私信
- **WorldState**：世界总状态
- **Fact**：客观事实
- **Belief**：角色认知 / 误解
- **Memory**：角色记忆
- **Relationship**：角色关系
- **StoryArc / StoryNode**：故事链节点
- **Event**：系统事件
- **Evidence**：证据
- **Rumor**：谣言

## 9. 关键设计原则

- **先保证一致性，再追求华丽生成**
- **先做小规模闭环，再扩展网站数量**
- **事实层与角色认知层必须分离**
- **所有重要行为应留下可追踪记录**
- **故事推进应由世界状态驱动，而不是纯随机文本拼接**
- **侦探事件必须嵌入日常社会行为中**

### 9.1 必须避免重蹈的旧问题

在此前项目经验中，已经暴露出两个高优先级风险，本项目必须从架构层面避免：

#### 问题 A：Python 文件堆叠，结构和引用关系混乱

曾出现所有 `.py` 文件堆在同一目录中的情况，导致：

- 模块职责不清
- `API`、业务逻辑、数据库调用、AI 调用混杂
- import 关系失控
- 循环依赖风险变高
- 后续接手成本高

**本项目的约束：**

- `api/` 只处理 HTTP 请求、响应和参数校验
- `services/` 只处理业务流程和用例编排
- `domain/` 只维护核心实体、值对象和规则
- `repositories/` 只定义数据访问抽象
- `infrastructure/` 只处理数据库、缓存、LLM、队列等外部依赖
- `simulation/` 只处理世界推进、事件调度和故事链驱动
- `schemas/` 只维护请求/响应与内部结构化数据模型

**硬性原则：**

1. 不允许把数据库操作直接散落在路由中
2. 不允许让 LLM 调用直接写入世界事实
3. 不允许工具函数目录演变成“什么都往里塞”的杂物间
4. 所有跨站行为必须先进入服务层或模拟层，再落数据库与事件日志

#### 问题 B：LLM 文本生成与系统真实状态脱节

典型情况：某个 AI 在帖子中声称“已经把文件发到网盘，并附上 ID 和密码”，但网盘系统中并没有真实存在对应文件记录。

这种问题的本质是：

- 先让模型自由生成文本
- 再把文本误当成真实世界事实
- 导致叙事系统与数据系统分裂

**本项目的约束：**

- 世界事实必须由后端动作系统创建
- 文本内容只能描述已存在或已验证的事实
- LLM 只能提出候选动作、生成表达、补充风格，不应直接创造系统事实

### 9.2 事实先行、文本后生成

这是本项目最重要的生成原则。

错误流程：

1. 直接让 LLM 写帖子
2. 帖子里提到某个文件、商品、证据或交易
3. 系统事后再尝试补这些对象

正确流程：

1. **角色提出意图**：例如“上传文件并在论坛分享”
2. **服务层执行动作**：真正创建文件记录、生成 `file_id`、访问令牌、提取码等
3. **事实入库**：把文件、帖子草稿、关联关系、事件日志写入数据库
4. **LLM 生成文本**：把已经存在的结构化事实传给模型，让模型生成符合角色口吻的帖子
5. **一致性校验**：检查文本中引用的 `file_id`、密码、链接、金额、物品名是否与数据库一致
6. **公开发布**：校验通过后才真正发布到论坛、商店、私信或新闻站

**一句话总结：**

- 系统事实不能由文本倒推
- 文本只能表达已经存在的事实
- LLM 负责表达，系统负责真实

### 9.3 后端基础分层建议

建议后端以 `FastAPI` 为主要 API 框架，采用下列结构：

- `api/`：对前端暴露 REST API，后续可扩展 WebSocket
- `services/`：封装“发帖”“回帖”“上传文件”“发布商品”“推进一轮模拟”等用例
- `domain/`：定义 `Agent`、`Event`、`Fact`、`Belief`、`StoryNode` 等核心模型
- `repositories/`：定义对 `Agent`、`Post`、`Listing`、`Event`、`Memory` 等数据的读写接口
- `infrastructure/db/`：放数据库连接、ORM 模型、持久化实现
- `infrastructure/llm/`：封装对不同大模型供应商的统一接口
- `simulation/`：处理 AI 社会的回合推进、事件注入、故事链调度
- `consistency/`：专门做上下文校验、知识边界校验和跨站引用校验

### 9.4 AI 行为执行规范

以后所有 AI 行为都应该遵循以下模式：

1. **先决策，不先发文**：AI 先产出结构化意图，而不是最终文本
2. **先执行动作，再写事实**：由服务层或模拟层执行业务动作
3. **先落事件，再可追踪**：动作必须形成事件链和审计记录
4. **最后生成表述文本**：论坛帖、私信、公告、新闻都应基于事实生成
5. **发布前校验一致性**：防止角色越权知道不该知道的信息，或引用不存在的对象

推荐把一次跨站行为拆成如下事件：

- `AgentIntentCreated`
- `WorldActionExecuted`
- `FactPersisted`
- `ContentDraftGenerated`
- `ConsistencyCheckPassed`
- `ContentPublished`

后续无论是普通日常行为，还是侦探事件、匿名爆料、伪证传播，都应走同样的链路。

## 10. 待确认问题

这些问题仍需后续明确：

- 后端技术栈已初步选择 `Python + FastAPI`，但数据库和任务队列方案仍待明确
- AI 调度是同步回合制还是异步事件驱动
- 世界状态先存内存、JSON 文件，还是数据库
- 故事链引擎是规则优先、LLM 优先，还是混合模式
- 是否允许用户直接干预世界状态
- 第一个可玩的垂直切片是哪一块

## 11. 对下一位 AI 的工作提示

如果你是新接手这个仓库的 AI，请优先做以下事情：

1. 先阅读本文件与根目录 `README.md`
2. 识别当前前后端真实代码状态，而不是假设功能已存在
3. 不要直接扩展成大而全系统，先做最小闭环原型
4. 任何新增设计都应保持“多网站 + 持续角色 + 故事一致性”三大核心
5. 开工前先更新本文件中的“变更日志 / 当前状态 / 下一步”

## 12. 当前状态摘要

- **产品方向：** 已初步明确
- **架构设计：** 已有交接文档和后端骨架级设计，仍需正式系统架构文档
- **后端实现：** 已落地最小 `FastAPI` 骨架、健康检查、世界摘要与演示发帖 API
- **前端实现：** 仍为模板
- **AI 叙事引擎：** 已有 `mock` LLM 接口与“事实先行、文本后生成”演示链路
- **侦探事件系统：** 未实现

## 13. 下一步（最近优先级）

建议最近一次开发从以下任务开始：

1. 输出正式系统架构文档
2. 设计核心实体和 JSON / TypeScript / Python 数据结构
3. 把 `InMemory` 仓储替换为真实数据库实现
4. 为论坛、商店、私信建立独立服务层与 API
5. 改造前端首页为项目控制台

## 14. 变更日志

### 2026-03-10

- 建立首版 AI 交接文档 `docs/AI_HANDOFF.md`
- 记录了项目目标、系统抽象、MVP 边界、当前仓库状态与下一步建议
- 当前仍处于需求澄清 / 架构准备阶段，尚未进入核心功能开发
- 补充旧项目失败经验：目录结构混乱、LLM 文本与真实系统状态脱节
- 明确本项目必须遵守“事实先行、文本后生成、事件链落库”的核心原则
- 增加后端分层建议与 AI 行为执行规范，供后续实现直接参考
- 在 `backend/` 下落地最小 `FastAPI` 骨架，包含 API、服务层、仓储、模拟引擎、数据库占位和 `mock` LLM 适配器
- 新增 `GET /api/v1/health`、`GET /api/v1/world/summary`、`POST /api/v1/world/demo-post`
- 增加 `backend/README.md`、`backend/requirements.txt` 与 `backend/tests/smoke_check.py`
- 已用 `backend\venv\Scripts\python.exe` 运行烟雾验证，确认核心路由可导入

---

## 15. 维护模板（后续每次开发后可复制更新）

可按以下格式追加：

```md
### YYYY-MM-DD
- 完成：
- 修改文件：
- 新决策：
- 遗留问题：
- 下一步：
```
